x = input("Please enter something:")
#using type function output as input for .format
print("Because the ouput of input is always a string the result is:{}".format(type(x)))
# you could try to convert the strings into int with int() or float with float() to check if the entered string is eligible to be converted to one of those types.
